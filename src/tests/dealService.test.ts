// import { DealService } from '../application/services/DealService';
// import { DealRepository } from '../infrastructure/firebase/dealRepo';
// import { Deal } from '../domain/entities/Deal';

// test('creates a new deal', async () => {
//   const dealRepo = new DealRepository();
//   const dealService = new DealService(dealRepo);
//   const deal = new Deal('1', 'Test Deal', 'Test Description', 10);

//   await dealService.createDeal(deal);
//   const deals = await dealService.getAllDeals();
//   expect(deals).toContainEqual(deal);
// });
